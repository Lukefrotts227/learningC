#include <stdio.h>
#define HEIGHT 12
#define WIDTH 11
#define LENGTH 12

int main(){
    int volume; 
    volume = HEIGHT * WIDTH * LENGTH; 
    printf("The area is...\n%i", volume);
    return 0; 
}